name: keystone
description: Aplicação para outorga de recursos hídricos.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.0

  pedantic: ^1.11.0

  # https://github.com/edufolly/folly_fields
  folly_fields:
    git:
      url: git://github.com/edufolly/folly_fields.git
      rev: 0.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
